# ==============================================
# Supabase Configuration - Hamsafar Mirza
# ==============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# Supabase Project URL
# Get from: Supabase Dashboard > Settings > API > Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (safe for client-side)
# Get from: Supabase Dashboard > Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (REQUIRED for seed script - bypasses RLS)
# Get from: Supabase Dashboard > Settings > API > Project API keys > service_role
# ⚠️ WARNING: Never expose this key in frontend code! Server-side only.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Data Source Mode
# Options: auto | supabase | mock
# - auto: Use Supabase if configured, fallback to mock data
# - supabase: Force Supabase (requires credentials)
# - mock: Always use mock data (no backend needed)
VITE_DATA_SOURCE=auto
